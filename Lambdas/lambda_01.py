# Write a lambda that accepts a single number and cubes it. Save it in a variable called cube.
cube = lambda x: x**3


def decrement_list(list1):
    return list(map(lambda n: n-1, list1))

print(decrement_list([1, 3, 4, 5, 11]))



